#pragma once

#include <string>
#include <sstream>

using namespace std;

string toa(int x);
int toi(string s);
string lowercase(string s);
inline bool isDelim(char ch);
void trim(string & s);